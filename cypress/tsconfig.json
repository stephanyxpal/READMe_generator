// This configuration file is used to set up TypeScript for the Cypress project.
// It extends the base tsconfig.json and includes all TypeScript and TSX files in the project.

{
    "extends": "../tsconfig.json",
    "include": ["**/*.ts", "**/*.tsx"]
  }